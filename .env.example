# Upstash Redis (REST) — cria em https://upstash.com e cola na Vercel em Settings → Environment Variables
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Opcional: em dev, se a API não estiver no mesmo host (ex.: só `vite`), define a base da API
# VITE_API_BASE=http://127.0.0.1:3000
