# Supabase — Project Settings → API (URL + service_role; a service_role só no servidor, nunca no Vite)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Opcional: em dev, se a API não estiver no mesmo host (ex.: só `vite`), define a base da API
# VITE_API_BASE=http://127.0.0.1:3000
